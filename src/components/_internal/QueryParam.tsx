"use client"

// import useCookieQueryParam from "@/app/hooks/_internal/useAffiliatorId"
const QueryParam = (): JSX.Element => {
    // const { affiliatorId } = useCookieQueryParam('a')
    return (
        <></>
    )
}

export default QueryParam